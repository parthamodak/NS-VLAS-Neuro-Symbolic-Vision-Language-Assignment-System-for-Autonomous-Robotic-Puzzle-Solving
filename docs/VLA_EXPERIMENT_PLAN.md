# VLA experiment plan

Compare the deterministic teacher with an actually fine-tuned VLA only after
valid demonstrations and a compatible checkpoint exist. Report per episode:
success, cube vertical displacement, duration, policy decisions, inference
latency, cube start position, and failure type. Do not report aggregate
statistics until multiple trials have been performed.
